---
name: ks-goldmine
description: >-
  本机金矿挖掘——对使用者自己电脑里的全部内容（项目、代码、文档、笔记、研究、语料、账号线索、自动化、经授权的聊天导出）做毫无遗漏、
  全面具体清晰细致的深度地毯式盘点，找出这个人真正擅长的点（可一个可多个）、真正感兴趣的点（可一个可多个），把二者交叉融合，
  再对照全网真实市场证据与三路对抗红队，产出针对此人的、有实质商业回报的个性化赚钱路径 Top N + 可直接动手的行动方案与交付物。
  触发：用户想「从我电脑里找出能赚钱的东西」「我到底擅长什么/该做什么副业/创业方向」「把本机资产交叉融合找商业价值」
  「盘点我所有项目看哪个能变现」「挖我自己的金矿」「给我个性化的赚钱路径 Top 3」，或以找变现/方向为目的要求对本机做「地毯式/毫无遗漏/全维度」的资产盘点。
  不触发：核查别人的网络宣称 → ks-deep-claim-audit；审博主靠不靠谱 → source-audit；本机安全体检 → ks-mac-security-audit；本机整理/迁移/同步类盘点、单个产品的定价文案 → 不触发。
  核心动作：只读盘点本机 → 机主画像（擅长/兴趣/交叉）→ 授权后本地分析人脉 → 外部赛道证据 → 多视角融合发散 → 评委 + 三路红队 + 反方 →
  Top N 报告（保守口径、过程指标、红线、可更正）→ 三审 → PDF → Top 1 的交付包。
---

# ks-goldmine · 本机金矿挖掘

> 把「我电脑里有什么」变成「我该向谁、按什么价、卖什么」。不是列资产清单，是找**这个人**擅长的、感兴趣的、二者交叉之后别人给不出的东西——再用市场证据和红队把它压到真能收钱的形态。

来源：2026-09-17～20 一次完整实跑（5 个 workflow、130+ agent、约 3,000 万 token）蒸馏；教训见 `references/lessons.md`。可选配套（同作者公开仓库，装了更好，没装也能跑）：`ks-100`（满血执行标准）、`ks-agent-team-review`（核验纪律）、`ks-deep-claim-audit`（核查别人的宣称）、`ks-anything-to-pdf`（出 PDF）。

输入：使用者给的目标与约束（例：「3 个月内合法月入 ≥X；本金/每周时间/所在地/主体/红线」）。

## 宿主适配（先读这一段再照 SOP 做）

本 skill 的编排词是抽象的，按你的宿主对应：

| 本文用词 | Claude Code | Codex | 其他助手 |
|---|---|---|---|
| 「派 workflow / 并行 agent」 | `Workflow` 工具（`parallel()` / `pipeline()`），或多个后台 `Agent` | `collaboration.spawn_agent` 并行派活 | 顺序派后台 agent，同样断点续做 |
| 「续跑同一个 run」 | `Workflow` 的 `resumeFromRunId` | 重跑同一脚本，已完成的输出文件会被跳过（断点续做保证） | 同左 |
| 「强模型」 | Opus（或更高） | 当前最强推理模型 | 同左 |
| 「快模型」 | Sonnet | 当前次一档模型 | 同左 |
| 「思考强度 xhigh」 | 会话级 effort 设 xhigh，子 agent 继承 | 对应的 reasoning 设置 | 能设就设最高 |
| 「记忆目录」`{MEM}` | `~/.claude/projects/*/memory` | 项目 `AGENTS.md` 与你的记忆文件 | 没有就删掉这一行 |
| 「心跳」 | `CronCreate` 30 分钟自唤醒 | 宿主的定时机制；没有就靠人工回来续 | 同左 |

下文凡写「Opus」读作强模型、「Sonnet」读作快模型。

---

## 铁律

1. **只读。** 全程不修改/移动/删除本机任何文件，不启动服务，不装依赖，不跑项目代码；唯一写入处是本次的项目目录。不碰生产系统、不续证书、不上架、不发消息、不联系任何人——这些只准备草稿，留给使用者拍板。
2. **凭据与隐私红线。** 不打开 .env/*cookie*/*secret*/*credential*/*.pem/*.key/*.p12/id_rsa*/token 类文件；任何密钥、口令、证件号不进任何输出。聊天记录、照片、证件、合同里的第三方信息不读正文（唯一例外：阶段 1b 经使用者明确授权的聊天导出，其中每人姓名与最多 26 条短片段会发给模型分类，授权前必须把这一点说清）；第三方姓名只留在本机线索文件（标「仅本机」），报告与对话里一律角色化。分析聊天导出必须先获使用者明确授权，且只分析他本人的账号。
3. **事实与推断分开。** 成熟度、用户数、收入、上线状态必须带证据（路径:行号 / 命令→输出 / URL+日期）；没证据写「未见证据」；猜测标「推断」。收入类外部说法分级（platform_verified > third_party_reported > self_reported > marketing），后两级不能单独支撑结论。收入结论只用**保守档**。
4. **客观双向。** 不附和「人人月入十万」，也不为显得冷静硬挑刺；做得好的地方明说。对使用者的短板要有证据才写（例如用资产卡里逐项的收入证据证明「工程很深但从不收钱」的模式）。
5. **必须有对抗层。** 三个立场不同的评委、三路红队（需求 / 成交与交付 / 法律与 IP）、一位独立给出最优组合的反方首席、一位工时压力测试员、交付前三位审查员。少一层不算完成。
6. **先扫再派。** 主线程先摸清目录结构、定范围，再派 workflow；同一时刻只跑一个 workflow；一个 agent 一个专属输出文件；agent 全部「断点续做」（先查输出文件是否已完成、边做边落盘、末尾 DONE 标记）；并发 ≤5 更稳；workflow 挂了就续跑同一个 run（见宿主适配表）。使用者要离开时才设 30 分钟心跳，完成后删；平时不设。没有并行编排能力的宿主用后台 agent 顺序派活，同样断点续做。
7. **状态文件。** 项目目录写 `00_STATE.md`：委托原话要点、约束、边界、进行中/已完成、待拍板、进度日志。每次被唤醒先读它。
8. **模型分级。** 盘点/取数/分类/需求与成交红队 → Sonnet；画像、发散、合并、评委、法律红队、反方、压力测试、三位审查员 → Opus；主线程亲自复核载重结论并对自己的偏差透明更正（我的偏差几乎全朝更乐观，规则见判据速查）。

---

## SOP（六个阶段，每阶段一个 workflow；产物布局见 `references/output_layout.md`）

### 阶段 0 · 定约束，建目录
问清或从使用者给的输入里取（每项一句话即可，不展开）：
- 目标：金额与期限；本金；现金流压力。
- 时间：每周可投入几小时；**现在同时在推几条线、哪些愿意冻结**（时间约束多半是分配问题，不是总量问题）。
- 主体与收款：所在地；用哪个主体收款，**它最后一次开票是什么时候、现在能不能开、有没有代持或未签的股权**（自述「有效」不算，要他自己去查一次）；海外收款通道。
- 已有的收款痕迹：**哪怕很小——谁付过、多少、怎么收的**（这是判断「从没收过钱」还是「收过小钱没报过高价」的唯一依据）。
- 合伙与共有 IP：**哪些资产不是他一个人的；关键伙伴是甲方、渠道还是合伙人**（三者的报价方式完全不同）。
- 边界：可卖什么、不可卖什么、明确不做的事；现有受众规模；聊天导出是否授权分析。
不问「兴趣是什么」和「语言能力」——问出来的不可信，由阶段 1 画像从行为证据里判。
**先告知量级再开跑**：标准档约 130+ 个 agent、数千万 token；让使用者选「标准」或「轻量」（8–10 个范围、L1/L2/L3/L8 四个视角、红队只打前 3 名、不做抄研究），写进 `00_STATE.md`。建 `<项目目录>/`，写 `00_STATE.md`。目标不现实（如本金×合理月化远低于目标）当场说，但仍继续。

### 阶段 1 · 本机只读盘点 → 资产卡 + 机主画像
1. `scripts/scan_machine.sh` 列出候选目录、文件数、最近修改日、**近 14 天修改文件数（注意力铺在几条线上的直接证据）**、**起止日期（兴趣「持续 ≥3 个月」的判据来源）**、项目标志、AI 助手记忆目录、自动化任务；默认根目录显示 0 时先确认终端有「文件与文件夹」访问权限。补看 Documents/Downloads/Desktop/应用/容器/外接盘/云盘归档。
2. 按主题切成 15–25 个范围（每范围 ≤3 路径，附「重点提示」与红线），每范围一个 Sonnet agent，prompt 用 `templates/phase1_asset_card.md`。每卡回答：是什么、成熟度+证据、起止时间、独特资产（带规模数字）、变现痕迹、当时想干什么为何停、离能收钱差什么、法律风险、3–5 个变现点子；**能两读的原话（收付款或开票方向、是否收费、甲方还是合伙）两种读法并列写**。
3. 覆盖度审计：一个 agent 用 find 找所有 git 仓库/项目标志文件，与已覆盖路径比对，上报遗漏（项目型/商业型才算），补盘。
4. 机主画像（Opus，最后跑，可引用所有卡）：`templates/phase1_owner_profile.md`。**必须产出三张清单：擅长点（可多个，A/B 级 + 证据 + 别人难抄在哪）、兴趣点（可多个，从反复投入却没人要求的事里找，持续 ≥3 个月才算真）、擅长 × 兴趣交叉表（交集 / 擅长不感兴趣 / 感兴趣不擅长）。** 交集是阶段 3 的优先原料。画像末尾给「一页纸结论」（他是什么样的人、最强三张牌、最大一个问题、路径第一判据）和「待机主一句话确认」清单（主线程抄进 `00_STATE.md` 待拍板）。

### 阶段 1b · 人脉线索（可选，须授权；统计在本地，分类时片段会发给模型）
**输入格式**：`<导出目录>/{private_friends,private_unknown,groups_md}/*.md`，每行 `- [YYYY-MM-DD HH:MM] 发送人: 内容`（huohuoer/wechat-cli 一类工具的格式）；导出本身不在本 skill 范围，别的 IM/工具先转成这个格式。**没有导出或不授权**：让使用者按分类字段手填 ≤30 个「可能付钱的人」（只写角色也行）存为 `results/batch_manual.json`，后续步骤照跑。若使用者授权分析自己的聊天导出：`scripts/wechat_stage_a.py` 做本地统计（千余个私聊约 2 分钟）→ `wechat_make_batches.py` 切批 → 每批一个 Sonnet agent 按 `templates/phase1b_wechat_classify.md` 分类（不联网、不读批次外文件）→ `wechat_aggregate.py` 汇总为 `leads_master.csv`（含姓名·仅本机）与 `needs_anonymized.md`（不含姓名，可交给后续 agent）。群只做统计与类型猜测，不读正文。关键产出：**暖决策者到底有几个、对方亲口说过的需求有几条**（分类字段里 `stated_need` 只填原话，与推断的 `need_hypothesis` 分开）——这两个数决定 Top N 是几门生意还是一条阶梯。

### 阶段 2 · 外部赛道证据（8–12 条赛道，Sonnet xhigh）
从资产卡与画像里推出候选赛道（加一条「基准率」赛道：同类人兼职做到目标的比例与中位时间，作为先验），每条一个 agent，prompt 用 `templates/phase2_external_research.md`。要的是价格基准、真实收入案例（分级）、获客渠道、失败模式、合规；每篇末尾「未用渠道及原因」。可选加一轮「抄也是一种智慧」：五路搜现在真的用 AI/该领域赚到钱的案例 → 对抗核实 → 可抄清单（3 个月内能抄 / 6 个月内 / 不适合）。抄研究轮两条硬规则：所有案例列去向表；总体结论的分母写送核数。**阶段 2 末尾设 1 个完整性批评员（Opus）**：读 03_、07_ 与画像，产出「该搜没搜 / 被静默丢弃 / 未评估的形态」，喂给阶段 3 的种子——它看的是研究层，与阶段 4 只读报告的 R3 不重复。

### 阶段 3 · 融合 → 合并 → 评委 → 红队 → 组合审查
`templates/phase3_fusion.md`。主线程先写自己的种子创意；8 个视角（含 **L8 擅长 × 兴趣**）各出 8 条，互不可见；合并为 ≤20 候选并分类（fast_cash / compounding / option_bet / ritual）；每条候选必填 **「替代品价格」**（现成软件每月多少钱能做到八成——过不了这道门的直接降级）与 **「第几天把带金额的报价发给一个真实的人」**（最重的判据）；三评委按固定 JSON 打 8 项分（delight 只在前七项三位评委均值都 ≥5 时计入，否则记 0）；前 6–8 名三路红队；反方首席；**主线程拿评委+红队+反方三份意见拟 Top N**。第二段：拟入 Top N 但没过红队的候选先补三路红队，再对全部过了红队的组合跑工时压力测试。评分表与红队表用 `scripts/score_table.py` 从结构化返回生成。

### 阶段 4 · Top N 报告 → 三审 → PDF
`templates/phase4_report_review.md`。一页纸结论在最前且能独立阅读；每条路径同一套小标题；收入只写保守档并附概率与算式；金额指标换成过程指标；红线清单；「我要更正的地方」；附录含评分表/红队表（脚本自动生成）、武器库清单（已用/未用及原因）、可直接用的模板。三位审查员（数字与证据 / 法律隐私与过度承诺 / 读者视角）→ 逐条处理 → 重拼 → PDF（可选：装了 `ks-anything-to-pdf` 就用它的 HTML 路径 + 使用者自己的视觉规范；没有就用 Chrome headless 打印 HTML；再没有就交 md）。报告「二、你这个人」放擅长 N 条 / 兴趣 N 条 / 交集 → 对应候选编号；第五章交代 L8 交集点子的去向（入选，或被哪一路红队否掉）。

### 阶段 5 · Top 1 的交付包（可选）
把第一档产品做成可直接使用的东西：一页纸报价单（客户版可从同一份 md 切出）、现场清单、按客群的行业包（含完整提示词与可机械判定的验收）、交付手册模板、课纲、答疑 FAQ、验收清单；从线索表筛首发名单并逐人写开场白（含姓名·仅本机）；relationship=partner 或阶段 0 登记过的合伙人、共有 IP 伙伴不进报价名单。使用者只差一个动作：把报价发出去。

### 收尾
统一归集总结（结论 → Top N → 这周要做的事 → 需要拍板的事 → 文件在哪；同一件事只讲一次）；更新 `00_STATE.md`；写进宿主的记忆/项目说明（约束、结论、被否掉的点子清单、自己的更正）；若设了心跳则删除。

---

## 模板占位符（主线程派 agent 前替换）
`{OUT}` 本次项目目录 ｜ `{NAME}` 机主称呼 ｜ `{TARGET}` 目标（金额+期限）｜ `{CONSTRAINTS}` 阶段 0 问到的约束一段话 ｜ `{TODAY}` 日期 ｜ `{MEM}` AI 助手的记忆目录（见宿主适配表；没有就删掉这一行）｜ `{ID}` `{LABEL}` `{PATHS}` `{NOTES}` 盘点范围的编号/名称/路径/重点提示 ｜ `{BATCH}` 批次名 ｜ `{INDUSTRY_LIST}` 行业清单（按使用者的人脉结构改，默认见 phase1b 模板）｜ `{TITLE}` `{QUESTIONS}` 赛道名与要查清的问题。

## 判据速查

- **路径第一判据**：能否在 14 天内逼使用者把一张带金额的报价单发给一个真实的人；凡「先做 demo/先建词表/先核接口再报价」的路径降权。
- **Top N 的形状**：暖决策者 <50 人时，Top N 多半是同一条阶梯的几档价位（第一档必须落在「不需要理由就能当场转账」的价位），不是几门生意。
- **眼前一亮的门槛**：擅长 × 兴趣的交集里、别人没把这两样放在一起做过的组合——但仍须过同一套评分与红队；创意加分，付费是门槛。
- **红线常客**（跑过一次就知道会撞的；合并员与红队用这一份，主线程粘贴进 prompt）：转售/代开境外账号、涉外调查、征信类尽调、对自然人尽调、二类医疗器械软件、个人静态收款码远程收经营款、算命类付费内容、共有 IP 未签、主体经营异常、对外发布 AI 内容的标识义务、个人微信自动化、把预付款写成「定金」（超合同额 20% 部分无效且违约双倍返还）。
- **不加总规则**：几条路径的收入相加前先查是否共用同一份工时/名单/本金；区间前先查口径（每周几小时、流水还是到手、币种）。外部证据的日期写「页面日期 + 事件日期」，两者不同时分开写。

## 未覆盖时怎么写
渠道不可用（登录墙、验证码、安全分类器拦截）→ 原样记 unavailable，不绕过；报告一页纸末尾一句指路 + 附录 E 写影响范围。
